apiVersion: v1
kind: Service
metadata:
  name: {{ include "marketdata.fullname" . }}
  labels:
    {{- include "marketdata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marketdata.selectorLabels" . | nindent 4 }}
